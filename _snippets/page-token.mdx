<ParamField query="page_token" type="string" optional>
  The Trackstar API retrieves the optimal number of items on a per integration basis.
  If there is additional data, the API will return a `next_token` that you can pass in to get the next page of results.
  Leave this field out to get the first page.
</ParamField>
