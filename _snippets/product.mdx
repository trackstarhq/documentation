<Expandable title="Product">

<ResponseField name="id" type="string">
  The unique ID of a product.
</ResponseField>

<ResponseField name="warehouse_customer_id" type="string">
  The ID of the merchant/tenant/customer that owns the product.
  Can be passed into the `/warehouse-customer/{warehouse_customer_id}` endpoint for more details.
</ResponseField>

<ResponseField name="created_date" type="string">
  The date the product was created. Returned in YYYY-MM-DDTHH:MM:SSZ format.
</ResponseField>

<ResponseField name="updated_date" type="string">
  The date the product was last updated. Returned in YYYY-MM-DDTHH:MM:SSZ format.
</ResponseField>

<ResponseField name="name" type="string">
</ResponseField>

<ResponseField name="sku" type="string or null">
</ResponseField>

<ResponseField name="gtin" type="string or null">
  The Global Trade Item Number of the product. i.e. UPC, EAN, or ISBN.
</ResponseField>

<ResponseField name="inventory_items" type="list of inventory_items">
    A list of inventory items associated with this product.
    <Expandable title="inventory_item">
      <ResponseField name="inventory_item_id" type="string">
      ID of the inventory item. Can be passed into the `/inventory/{inventory_item_id}` endpoint for more details.
      </ResponseField>

      <ResponseField name="sku" type="string or null">
      </ResponseField>

      <ResponseField name="unit_quantity" type="int">
      The number of units of the inventory item in the product.
      </ResponseField>
    </Expandable>
</ResponseField>

<ResponseField name="is_kit" type="bool">
</ResponseField>

</Expandable>
