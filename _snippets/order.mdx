<Expandable title="Order">
  <ResponseField name="id" type="string">
    The unique ID of an order.
  </ResponseField>

  <ResponseField name="created_date" type="string">
    The date the order was created. Returned in YYYY-MM-DDTHH:MM:SSZ format.
  </ResponseField>

  <ResponseField name="updated_date" type="string">
    The date the order was last updated. Returned in YYYY-MM-DDTHH:MM:SSZ format.
  </ResponseField>

  <ResponseField name="reference_id" type="string">
    An external unique identifier for the order. Defaults to `id` if client has not defined one.
  </ResponseField>

  <ResponseField name="order_number" type="string">
    A user-friendly identifier for their order. Defaults to reference_id if not provided.
  </ResponseField>

  <ResponseField name="status" type="string">
    Status of the order. See the [Order definition](/wms-api/orders/info) for more details.
  </ResponseField>

  <ResponseField name="channel" type="string or null">
    The ID of the channel the order was placed through, such as your Shopify store.
  </ResponseField>

  <ResponseField name="trading_partner" type="string or null">
    For retail/ EDI orders, the trading partner who placed the order (e.g. Target, Walmart).
  </ResponseField>

  <ResponseField name="shipping_method" type="string or null">
    The method by which the warehouse will ship the order to the customer.
    Can be passed into the `/shipping-methods/{shipping_method_id}` endpoint for more details.
  </ResponseField>

  <ResponseField name="invoice_currency_code" type="string">
    The currency used for this order. If not provided explicitly, defaults to USD.
  </ResponseField>

  <ResponseField name="line_items" type="list of line items">
      A list of the products included in the order.
      <Expandable title="Line Item" defaultOpen="true">

      <ResponseField name="product_id" type="string">
        Can be passed in to the `/products/{product_id}` endpoint for more details.
      </ResponseField>

      <ResponseField name="sku" type="string or null">
      </ResponseField>

      <ResponseField name="quantity" type="integer">
      </ResponseField>

      <ResponseField name="unit_price" type="float or null">
        The price of the product in the currency specified by `invoice_currency_code`.
      </ResponseField>

      </Expandable>
  </ResponseField>

  <ResponseField name="shipments" type="list of shipments">
    A list of the shipments sent out to the customer.
    <Expandable title="Shipment" defaultOpen="true">

      <ResponseField name="shipment_id" type="string">
        The ID of the shipment.
      </ResponseField>

      <ResponseField name="warehouse_id" type="string">
        The warehouse this order was shipped from.
        Can be passed in to the `/warehouses/{warehouse_id}` endpoint for more details.
      </ResponseField>

      <ResponseField name="shipped_date" type="string">
        The date the shipment was sent. Returned in YYYY-MM-DDTHH:MM:SSZ format.
      </ResponseField>

      <ResponseField name="line_items" type="list of line items">
        A list of the inventory items included in the shipment.
        <Expandable title="Line Item" defaultOpen="true">

          <ResponseField name="inventory_item_id" type="string">
            The ID of the inventory item.
          </ResponseField>

          <ResponseField name="sku" type="string or null">
            The SKU of the inventory item.
          </ResponseField>

          <ResponseField name="quantity" type="integer">
            The quantity of the inventory item shipped.
          </ResponseField>
        </Expandable>
      </ResponseField>

      <ResponseField name="ship_to_address" type="object">
        The address the order was shipped to.

        <Expandable title="Address" defaultOpen="true">

          <ResponseField name="address1" type="string or null">
            The first line of the address the order was shipped to.
          </ResponseField>

          <ResponseField name="address2" type="string or null">
            The second line of the address the order was shipped to.
          </ResponseField>

          <ResponseField name="city" type="string or null">
            The city the order was shipped to.
          </ResponseField>

          <ResponseField name="state" type="string or null">
            The state the order was shipped to.
          </ResponseField>

          <ResponseField name="postal_code" type="string or null">
            The postal code the order was shipped to.
          </ResponseField>

          <ResponseField name="country" type="string or null">
            The country the order was shipped to.
          </ResponseField>

        </Expandable>

      </ResponseField>

      <ResponseField name="packages" type="list of packages">
        A list of packages included in the shipment.
        <Expandable title="Package" defaultOpen="true">

          <ResponseField name="tracking_number" type="string or null">
            The tracking number for the shipment.
          </ResponseField>

          <ResponseField name="tracking_url" type="string or null">
            The URL for the tracking number.
          </ResponseField>

          <ResponseField name="carrier" type="string or null">
            The carrier used to ship the order.
          </ResponseField>

          <ResponseField name="scac" type="string or null">
            The Standard Carrier Alpha Code for the carrier used to ship the order.
          </ResponseField>

          <ResponseField name="measurements" type="object">
            The measurements of the package.

            <Expandable title="Measurements" defaultOpen="true">
              <ResponseField name="length" type="float or null">
                The length of the package.
              </ResponseField>
      
              <ResponseField name="width" type="float or null">
                The width of the package.
              </ResponseField>
      
              <ResponseField name="height" type="float or null">
                The height of the package.
              </ResponseField>

              <ResponseField name="unit" type="string or null">
                The unit of measurement for the package.
              </ResponseField>

              <ResponseField name="weight" type="float or null">
                The weight of the package.
              </ResponseField>

              <ResponseField name="weight_unit" type="string or null">
                The unit of measurement for the weight of the package.
              </ResponseField>
            </Expandable>

            <ResponseField name="boxes" type="list of boxes">
              A list of boxes included in the package. (AKA cartons)
              <Expandable title="Box" defaultOpen="true">
                <ResponseField name="box_id" type="string">
                  The ID of the box.
                </ResponseField>
                <ResponseField name="measurements" type="object">
                  The measurements of the box.

                  <Expandable title="Measurements" defaultOpen="true">
                    <ResponseField name="length" type="float or null">
                      The length of the box.
                    </ResponseField>
            
                    <ResponseField name="width" type="float or null">
                      The width of the box.
                    </ResponseField>
            
                    <ResponseField name="height" type="float or null">
                      The height of the box.
                    </ResponseField>

                    <ResponseField name="unit" type="string or null">
                      The unit of measurement for the box.
                    </ResponseField>

                    <ResponseField name="weight" type="float or null">
                      The weight of the box.
                    </ResponseField>
                    <ResponseField name="weight_unit" type="string or null">
                      The unit of measurement for the weight of the box.
                    </ResponseField>

                  </Expandable>

                </ResponseField>

                <ResponseField name="line_items" type="list of line items">
                  A list of the inventory items included in the box.
                  <Expandable title="Line Item" defaultOpen="true">
                    <ResponseField name="inventory_item_id" type="string">
                      The ID of the inventory item.
                    </ResponseField>

                    <ResponseField name="sku" type="string or null">
                      The SKU of the inventory item.
                    </ResponseField>

                    <ResponseField name="quantity" type="integer">
                      The quantity of the inventory item shipped.
                    </ResponseField>

                    <ResponseField name="lot_id" type="string or null">
                      The ID of the lot the inventory item was picked from.
                    </ResponseField>

                    <ResponseField name="expiration_date" type="string or null">
                      The expiration date of the lot the inventory item was picked from.
                    </ResponseField>
                  </Expandable>
                </ResponseField>
              </Expandable>
            </ResponseField>
          </ResponseField>
        </Expandable>
      </ResponseField>
    </Expandable>
  </ResponseField>
</Expandable>