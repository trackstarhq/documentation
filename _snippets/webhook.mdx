<Expandable title="Webhook">

<ResponseField name="id" type="string">
  The unique ID of a webhook.
</ResponseField>

<ResponseField name="integration_name" type="string">
  The WMS that the webhook is configured to listen to.
</ResponseField>

<ResponseField name="topic" type="string">
  The event that the webhook is configured to listen for.
  See the [Webhooks definition](/webhooks/info) for more details.
</ResponseField>

<ResponseField name="url" type="string">
  The URL that the webhook will send messages to.
</ResponseField>

</Expandable>
