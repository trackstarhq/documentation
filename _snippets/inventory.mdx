<Expandable title="Inventory Item">

<ResponseField name="id" type="string">
  The unique ID of an inventory item.
</ResponseField>

<ResponseField name="warehouse_customer_id" type="string or null">
  The ID of the merchant/tenant/customer that owns the inventory item.
  Can be passed into the `/warehouse-customers/{warehouse_customer_id}` endpoint for more details.
</ResponseField>

<ResponseField name="created_date" type="string">
  The date the inventory item was created. Returned in YYYY-MM-DDTHH:MM:SSZ format.
</ResponseField>

<ResponseField name="updated_date" type="string">
  The date the inventory item was last updated. Returned in YYYY-MM-DDTHH:MM:SSZ format.
</ResponseField>

<ResponseField name="sku" type="string or null">
</ResponseField>

<ResponseField name="name" type="string">
</ResponseField>

<ResponseField name="active" type="boolean">
</ResponseField>

<ResponseField name="awaiting" type="integer">
  Inventory that the warehouse is awaiting delivery of.
</ResponseField>

<ResponseField name="onhand" type="integer">
  The total amount of inventory in the warehouse.
</ResponseField>

<ResponseField name="committed" type="integer">
  Inventory that is already assigned to orders.
</ResponseField>

<ResponseField name="unfulfillable" type="integer">
  Inventory that can't be fulfilled for orders (e.g. damaged/quarantined).
</ResponseField>

<ResponseField name="fulfillable" type="integer">
  Inventory that can be fulfilled for orders.
</ResponseField>

<ResponseField name="unsellable" type="integer">
  Inventory that is being held from sales channels (e.g. reserved)
</ResponseField>

<ResponseField name="sellable" type="integer">
  Inventory that is available in sales channels.
</ResponseField>

<ResponseField name="substitute_skus" type="list">
  A list of inventory item SKUs that can replace this item if it runs out of stock
</ResponseField>

<ResponseField name="inventory_by_warehouse_id" type="dictionary">

  Inventory broken down by warehouse.

  <Expandable title="Inventory by Warehouse">

  <ResponseField name="warehouse_id_as_key" type="string">

    Warehouse ID.

    <Expandable name="warehouse_info" type="object">

        <ResponseField name="onhand" type="integer">
            Total quantity in the warehouse.
        </ResponseField>
        <ResponseField name="awaiting" type="integer or null">
            Inventory expected to arrive to the warehouse. Will be None if data not provided by integration.
        </ResponseField>
        <ResponseField name="committed" type="integer or null">
            Inventory in the warehouse that is already assigned to orders. Will be None if data not provided by integration.
        </ResponseField>
        <ResponseField name="unfulfillable" type="integer or null">
            Inventory in the warehouse that is damaged, quarantined, etc. Will be None if data not provided by integration.
        </ResponseField>
        <ResponseField name="fulfillable" type="integer or null">
            Inventory in the warehouse that can be fulfilled and shipped for orders. Will be None if data not provided by integration.
        </ResponseField>
        <ResponseField name="unsellable" type="integer or null">
            Inventory in the warehouse that is being held from sales channels (e.g. reserved). Will be None if data not provided by integration.
        </ResponseField>
        <ResponseField name="sellable" type="integer or null">
            Inventory in the warehouse that is available in sales channels. Will be None if data not provided by integration.
        </ResponseField>

    </Expandable>

  </ResponseField>

  </Expandable>

</ResponseField>

<ResponseField name="lots" type="list">

  A lot is a specific batch of inventory items that are stored together and expire together.
  If an item doesn't expire, it is not allocated to a lot.

  <Expandable title="Lot">

  <ResponseField name="lot_id" type="string">
    Lot ID.
  </ResponseField>

  <ResponseField name="onhand" type="integer">
    The total amount of inventory in the lot.
  </ResponseField>

  <ResponseField name="expiration_date" type="string">
    Date that the lot expires. Returned in YYYY-MM-DDTHH:MM:SSZ format.
  </ResponseField>

  <ResponseField name="warehouse_id" type="string">
    ID of the warehouse the lot is kept in.
  </ResponseField>

  </Expandable>

</ResponseField>

<ResponseField name="measurements" type="object">

  The measurements of the inventory item.

  <Expandable name="warehouse_info" type="object">

      <ResponseField name="length" type="float or null">
          Length of the inventory item.
      </ResponseField>
      <ResponseField name="width" type="float or null">
          Width of the inventory item
      </ResponseField>
      <ResponseField name="height" type="float or null">
          Height of the inventory item
      </ResponseField>
      <ResponseField name="unit" type="string or null">
          Unit of measurement for the dimensions of the inventory item. Will either be `in` or `cm`.
      </ResponseField>
      <ResponseField name="weight" type="float or null">
          Weight of the inventory item
      </ResponseField>
      <ResponseField name="weight_unit" type="string or null">
          Unit of measurement for the weight of the inventory item. Will either be `lb` or `kg`.
      </ResponseField>

  </Expandable>

</ResponseField>


</Expandable>
